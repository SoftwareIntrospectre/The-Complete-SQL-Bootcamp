-- How can you produce an ordered list of the fist 10 surnames in the members table?
-- This list must not contain duplicates.

SELECT DISTINCT surname
FROM cd.members
ORDER BY surname -- creates the "ordered list" aspect, in alphabetical order
LIMIT 10;
